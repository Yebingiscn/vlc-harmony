--- a/HPKBUILD
+++ b/HPKBUILD
@@ -39,6 +39,30 @@ prepare() {
             return -1
         fi
         cd $builddir
+        patch -p1 < ../0001-avcodec-respect-disabled-hardware-decoding.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
+        patch -p1 < ../0002-avcodec-fallback-to-software-on-hw-start-failure.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
+        patch -p1 < ../0003-vcd-mode1-2048-iso.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
+        patch -p1 < ../0004-bluray-seek-fix.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
+        patch -p1 < ../0005-vcd-iso9660-no-cue.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
+        patch -p1 < ../0007-vlc-ohos-surface-clocked-present.patch >> $publicbuildlog 2>&1
+        if [ $? -ne 0 ]; then
+            return -1
+        fi
         cd $OLDPWD
         cloneflag=false
     fi
