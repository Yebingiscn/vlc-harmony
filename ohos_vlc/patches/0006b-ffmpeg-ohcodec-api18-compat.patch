diff --git a/libavcodec/ohdec.c b/libavcodec/ohdec.c
--- a/libavcodec/ohdec.c
+++ b/libavcodec/ohdec.c
@@ -1048,9 +1048,12 @@ static void oh_decode_update_playback_policy(AVCodecContext *avctx)
     if (!format)
         return;
 
-    const OH_FrameRetentionMode retention_mode = enable_smart_fluency
-        ? OH_FRAME_RETENTION_MODE_ADAPTIVE
-        : OH_FRAME_RETENTION_MODE_FULL;
+    /* The retention keys and enum were added in API 26. Resolve the keys at
+     * runtime and use their stable int32 wire values so this build remains
+     * source-compatible with the API 18 NDK. This block is unreachable when
+     * the running system does not export both keys. */
+    const int32_t retention_mode = enable_smart_fluency ? 1 : 0;
     bool valid = true;
     if (effective_frame_rate > 0.0 && s->keys.frame_rate)
         valid = OH_AVFormat_SetDoubleValue(format, s->keys.frame_rate,
